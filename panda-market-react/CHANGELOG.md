# 판다마켓

- 배포주소 : https://panda-market-kr.netlify.app/

## 2025.08.08. 스프린트 5 진행

1. 리팩토링 진행
   > 검색, 페이지네이션, 반응형, 최신순, 좋아요순 기능들이 개별적으로 동작할 때는 정상적으로 동작되지만 복합적으로 진행될 때는 문제가 발생함을 자체 QA를 통해 확인
   > 이를 개선하기 위해 검색 서치와 관련된 항목들을 상품 리스트 조회를 하나의 함수(useProducts.js)에서 처리하여 해결
   > 이미지 src에 'example.com/...' 값이 들어가는 데이터의 경우 이미지 엑스박스 처리가 되는 이슈 발생, 이를 해결하기 위해 기본값 url로 에러 처리 완료
   > ProductListpage.js 안의 코드가 많아짐에 따라 데이터 불러오는 함수를 커스텀 훅으로 분리
   > 로딩 및 api 통신 중 에러 발생 시 화면에 메시지 추가
   > Pagination.jsx 에서 Link 태그를 button 태그로 변경, 쿼리스트링을 사용할 이유를 찾지 못했고 페이지 이동이 아니라고 생각하여 button 태그로 판매중인 상품의 currentPage 상태만 변경.
   > useWindowWidth.js 커스텀 훅 삭제, useDeviceType 커스텀 훅을 만들어 화면 너비에 따른 디바이스 값만 반환, 화면 조정에 따른 불필요한 렌더 제거

## 2025.08.07. 스프린트 5 진행

1. 드랍다운 컴포넌트 작업
2. 반응형 작업
3. 404페이지 추가
4. 가로 너비 커스텀 훅
5. 드랍다운 외부 영역 클릭 시 옵션 닫기 커스텀 훅 작업

## 2025.08.06. 스프린트5 진행

1. 폴더구조 세팅
2. 필요한 컴포넌트 및 Prettierrc 설정
3. 페이지네이션 구현
4. 검색 기능 구현

## 2025.08.05. 스프린트5 시작

1. vite 빌드 툴로 react 프로젝트 생성
2. 기존에 작업했던 header, footer에 대한 소스 이전

## 2025.07.31. 17:52 스프린트4 완료

## 2025.07.31. 코드리뷰 반영 내용

1. 미리보기 설정에서 설명 부분 수정 “일상에서 모든 물건을 거래해보세요”
2. 상단 배너 이미지, 하단 배너 이미지 : PC, Tablet 사이즈의 이미지 크기 고정으로 수정.
3. 관심사 분리, 순환 참조 수정, 모듈화 (모달) 등 전반적인 자바스크립트 로직 수정

## 2025.07.28. 코드리뷰 반영 내용

1. 회원가입 & 로그인 login.css -> auth.css로 파일명 변경
2. style.css -> main.css 파일명 변경
